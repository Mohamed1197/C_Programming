#include <stdio.h>

void main(void)
{
	/* Define Int Variable without initialization */
	int x ;
	/* Display a message to the user to enter a value */
	printf("\nPlease Enter the Value : ");
	/* wait for the user to enter the value */
	scanf("%d",&x);
	/* Display the value entered by the user */
	printf("the Value is : %d \n",x);		
	
}